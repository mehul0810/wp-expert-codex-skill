interface:
  display_name: "WP Expert"
  short_description: "Thin router for ambiguous WordPress work"
  default_prompt: "Use $wp-expert only when WordPress work is ambiguous; otherwise use the plugin, theme, site, portfolio, product, contributor, or content specialist."
